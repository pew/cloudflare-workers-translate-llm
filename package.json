{
  "name": "translate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}'",
    "deploy": "npm run format && wrangler deploy",
    "start": "wrangler dev --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2",
    "wrangler": "^3.19.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.37"
  }
}
